package cards;

import static org.junit.Assert.*;

import org.junit.Test;

public class PokerDeckTest {

	@Test
	public void test() {
		PokerDeck deck = new PokerDeck();
		for(int i = 1; i < 12;  i++) {
			assertEquals(deck.getCard(0).getCardColor(), deck.getCard(i).getCardColor());
		}
	}

}
